# `/goal` Launcher Prompt

Execute the complete master specification at `/Users/midir/Documents/ps1 games/MASTER_GOAL_PROMPT.md`. Read it first and treat it as the authoritative, decision-complete contract, including the 2026-07-19 hardware pivot.

Re-verify current state, then rebuild the existing private repository `oh-ashen-one/elden-ring-3ds-demake` for the user's exact Japanese original Nintendo 3DS (CTR-001). Remove every New-3DS-only startup, control, CPU, documentation, and acceptance assumption while preserving the complete 5–8 minute vertical slice. Use original-3DS controls from the master prompt, retain optional New-family compatibility only where it cannot hide an Old-3DS defect, and keep the official devkitPro asset pipeline, tests, CI, diagnostics, Wi-Fi netloading, persistent SD installation, and video-evidence workflow healthy.

Preserve unrelated user work and never include ripped commercial assets, credentials, console-unique files, or SD backups. Follow current official documentation for toolchain and homebrew operations. Make coherent commits and push them to the private repository.

The console is present but stock/not homebrewed. Complete every safe local/GitHub rebuild task first. At the device boundary, request the exact System Settings version, then follow the current official 3DS Hacks Guide for that exact model/version; never improvise stale exploit steps. Preserve the existing 32 GB SD card's `Nintendo 3DS` data and never commit console-unique files or backups.

Persist until every acceptance criterion is genuinely satisfied. A build, CI run, package, SD copy, or emulator boot is not physical proof. Do not mark complete until the rebuilt `.3dsx` passes `3dslink`, offline SD launch, and three consecutive cold-boot full playthroughs on the user's CTR-001 with the required stability, 30 FPS target/no sustained drops below 24 FPS, memory, audio, transition, edge-case, and uncut-video evidence checks.

Launcher text character count: 1,958 (below the 4,000-character limit).

# `/goal` Launcher Prompt

Execute the complete master specification at `/Users/midir/Documents/ps1 games/MASTER_GOAL_PROMPT.md`. Read that file first and treat it as the authoritative, decision-complete contract for this goal.

Re-verify its starting-state claims, then autonomously create the private GitHub repository `oh-ashen-one/elden-ring-3ds-demake`, install/configure the official devkitPro 3DS toolchain as permitted, implement the native C++ New Nintendo 3DS homebrew vertical slice, add its original asset pipeline, tests, CI, documentation, performance instrumentation, Mac-to-3DS Wi-Fi netloading, persistent SD-card installation, and milestone/video-evidence workflow.

Preserve unrelated user work and never include ripped commercial assets, credentials, console-unique files, or SD backups. Follow current official documentation for toolchain and homebrew operations. Make coherent commits and push them to the private repository.

The console is not connected now: complete every safe local and GitHub task first. At the physical-device boundary, request only the minimal console/model/homebrew/network interaction needed, then resume. Never substitute emulator or compilation success for real hardware proof.

Persist across goal turns until every acceptance criterion and the Definition of Done in the master prompt is genuinely satisfied. Do not silently reduce the vertical-slice scope or physical-hardware gate, and do not mark the goal complete until the `.3dsx` has passed both `3dslink` and SD-card launch plus three consecutive cold-boot full playthroughs on the user's physical New Nintendo 3DS-family console with the required stability, performance, memory, and evidence checks.

Launcher text character count: 1,681 (below the 4,000-character limit).
